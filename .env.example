# ── Backend de AutoLibre ─────────────────────────────────────────────────────
# A donde manda /api/provider las solicitudes de partner. Sin NEXT_PUBLIC_ a
# proposito: solo la usa la ruta de servidor, no tiene por que viajar al bundle.
# Si falta, el alta de proveedores falla con un mensaje explicito al arrancar la
# request — no en silencio.
AUTOLIBRE_API_URL=http://localhost:3000

# ── Formulario de soporte (Resend) ───────────────────────────────────────────
RESEND_API_KEY=
SUPPORT_EMAIL_TO=

# ── Supabase (early access) ──────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# ── SEO ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# ── Google Sheets ────────────────────────────────────────────────────────────
# YA NO SE USAN. El alta de proveedores dejo de escribir en la planilla y ahora
# va al backend (partner_applications). Se dejan anotadas porque las credenciales
# siguen haciendo falta UNA vez mas: para correr el export del Sheet
# (autolibre-backend-hex/migrations/legacy/export-provider-sheet.mjs), que es lo
# que rescata el funnel historico antes de apagar la planilla.
# GOOGLE_SHEETS_SPREADSHEET_ID=
# GOOGLE_SERVICE_ACCOUNT_EMAIL=
# GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
