// Fallback for the @modal slot when no route is intercepted (initial load,
// hard refresh, or any route other than /proveedores navigated to directly).
export default function Default() {
  return null;
}
